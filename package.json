{
  "name": "echelon-root",
  "version": "1.0.0",
  "private": true,
  "description": "Echelon Google Ads Management System",
  "scripts": {
    "build": "./build.sh",
    "install-frontend": "cd frontend && npm install --force",
    "build-frontend": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 